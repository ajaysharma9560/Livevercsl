{
    "version": 2,
    "builds": [
        {
            "src": "config.json",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/config.json",
            "dest": "/config.json"
        }
    ]
}
